http {
    server {
        server_name localhost;
        listen 8080;

        root tests/www;

        location /1/ {
            autoindex on;
        }
    }

    server {
        server_name localhost1;
        listen 8080;

        root tests/www/1;
    }

    server {
        server_name localhost2;
        listen 8080;

        root tests/www/2;
    }

    server {
        server_name localhost;
        listen 8081;

        root tests/www;
    }
}
